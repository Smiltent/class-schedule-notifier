
services:
  # app:
  #   image: oven/bun:latest
  #   restart: always
  #   working_dir: /app
  #   volumes:
  #     - .:/app
  #   depends_on:
  #     - database
  #   ports:
  #     - "3000:3000"
  #   command: ["bun", "index.ts", "--debug"]
  #   networks:
  #     - app-internal

  database:
    image: mongo:latest
    restart: always
    environment:
      - MONGO_INITDB_DATABASE=schedule
    volumes:
      - mongodb:/data/db
    ports:
      - "27017:27017"  ##  DISABLE IN PRODUCTION
    networks:
      - app-internal

volumes:
  mongodb:

networks:
  app-internal:
    driver: bridge