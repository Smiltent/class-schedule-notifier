
<%- include('./components/$header.ejs') %>

<div class="main flex-col">
    <h1>schedule</h1>
    <a href="/class">lookup class</a>
    <a href="/teacher">lookup teacher</a>
    <a href="/classroom">lookup classroom</a>
</div>

<% if (user.role == 'admin' || user.role == 'manager') { %>
    <div class="main flex-col">
        <h2>special</h2>
        <% if (user.role == 'admin') { %>
            <a href="/admin">goto admin panel</a>
        <% } %>
    </div>
<% } %>
